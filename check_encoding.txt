-- ============================================
-- FAR7I DATABASE SCHEMA - COMPLETE IMPLEMENTATION
-- ============================================

-- 1. CREATE REUSABLE TRIGGER FUNCTION
-- ============================================
CREATE OR REPLACE FUNCTION update_updated_at_column()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = NOW();
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- 2. CREATE ROLE SYSTEM (SECURE APPROACH)
-- ============================================
CREATE TYPE public.app_role AS ENUM ('admin', 'provider', 'client');

CREATE TABLE public.user_roles (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
    role app_role NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    UNIQUE (user_id, role)
);

ALTER TABLE public.user_roles ENABLE ROW LEVEL SECURITY;

-- Security definer function to check roles (prevents RLS recursion)
CREATE OR REPLACE FUNCTION public.has_role(_user_id UUID, _role app_role)
RETURNS BOOLEAN
LANGUAGE SQL
STABLE
SECURITY DEFINER
SET search_path = public
AS $$
  SELECT EXISTS (
    SELECT 1
    FROM public.user_roles
    WHERE user_id = _user_id
      AND role = _role
  )
$$;

-- RLS Policies for user_roles
CREATE POLICY "Users can view their own roles"
    ON user_roles FOR SELECT
    TO authenticated
    USING (user_id = auth.uid());

CREATE POLICY "Admins can manage all roles"
    ON user_roles FOR ALL
    TO authenticated
    USING (public.has_role(auth.uid(), 'admin'))
    WITH CHECK (public.has_role(auth.uid(), 'admin'));

-- 3. CLIENT WAITLIST TABLE
-- ============================================
CREATE TABLE IF NOT EXISTS public.client_waitlist (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email VARCHAR(255) NOT NULL UNIQUE,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_client_waitlist_created_at ON client_waitlist(created_at DESC);

ALTER TABLE client_waitlist ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Anyone can insert into client waitlist"
    ON client_waitlist FOR INSERT
    TO anon, authenticated
    WITH CHECK (true);

CREATE POLICY "Admins can view all client waitlist entries"
    ON client_waitlist FOR SELECT
    TO authenticated
    USING (public.has_role(auth.uid(), 'admin'));

-- 4. PROVIDER WAITLIST TABLE
-- ============================================
CREATE TABLE IF NOT EXISTS public.provider_waitlist (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name VARCHAR(255) NOT NULL,
    email VARCHAR(255) NOT NULL UNIQUE,
    category VARCHAR(50) NOT NULL,
    wilaya VARCHAR(50) NOT NULL,
    phone VARCHAR(20),
    website TEXT,
    status VARCHAR(20) DEFAULT 'pending',
    notes TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    CONSTRAINT provider_waitlist_category_check CHECK (
        category IN ('photographe', 'dj', 'salle', 'traiteur', 'decoration', 'patisserie', 'coiffure', 'location-voiture', 'autre')
    ),
    CONSTRAINT provider_waitlist_status_check CHECK (
        status IN ('pending', 'approved', 'rejected')
    )
);

CREATE INDEX IF NOT EXISTS idx_provider_waitlist_category_wilaya ON provider_waitlist(category, wilaya);
CREATE INDEX IF NOT EXISTS idx_provider_waitlist_status ON provider_waitlist(status, created_at DESC);

DROP TRIGGER IF EXISTS update_provider_waitlist_updated_at ON provider_waitlist;
CREATE TRIGGER update_provider_waitlist_updated_at
    BEFORE UPDATE ON provider_waitlist
    FOR EACH ROW
    EXECUTE FUNCTION update_updated_at_column();

ALTER TABLE provider_waitlist ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Anyone can insert into provider waitlist"
    ON provider_waitlist FOR INSERT
    TO anon, authenticated
    WITH CHECK (true);

CREATE POLICY "Admins can view and manage provider waitlist"
    ON provider_waitlist FOR ALL
    TO authenticated
    USING (public.has_role(auth.uid(), 'admin'))
    WITH CHECK (public.has_role(auth.uid(), 'admin'));

-- 5. BLOG ARTICLES TABLE
-- ============================================
CREATE TABLE IF NOT EXISTS public.blog_articles (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    slug VARCHAR(255) NOT NULL UNIQUE,
    title VARCHAR(500) NOT NULL,
    category VARCHAR(50) NOT NULL,
    read_time VARCHAR(20) NOT NULL,
    image_url TEXT NOT NULL,
    content TEXT,
    excerpt TEXT,
    author_name VARCHAR(255),
    author_avatar TEXT,
    author_bio TEXT,
    tags TEXT[],
    published_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    views INTEGER DEFAULT 0,
    featured BOOLEAN DEFAULT false,
    status VARCHAR(20) DEFAULT 'draft',
    CONSTRAINT blog_articles_category_check CHECK (
        category IN ('tendances', 'conseils', 'inspiration','g̢teaux', 'budget', 'organisation', 'temoignages')
    ),
    CONSTRAINT blog_articles_status_check CHECK (
        status IN ('draft', 'published', 'archived')
    )
);

CREATE INDEX IF NOT EXISTS idx_blog_articles_slug ON blog_articles(slug);
CREATE INDEX IF NOT EXISTS idx_blog_articles_category_published ON blog_articles(category, published_at DESC);
CREATE INDEX IF NOT EXISTS idx_blog_articles_status_published ON blog_articles(status, published_at DESC);
CREATE INDEX IF NOT EXISTS idx_blog_articles_featured ON blog_articles(featured, published_at DESC) WHERE featured = true;

DROP TRIGGER IF EXISTS update_blog_articles_updated_at ON blog_articles;
CREATE TRIGGER update_blog_articles_updated_at
    BEFORE UPDATE ON blog_articles
    FOR EACH ROW
    EXECUTE FUNCTION update_updated_at_column();

ALTER TABLE blog_articles ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Anyone can view published articles"
    ON blog_articles FOR SELECT
    TO anon, authenticated
    USING (status = 'published');

CREATE POLICY "Admins can manage all articles"
    ON blog_articles FOR ALL
    TO authenticated
    USING (public.has_role(auth.uid(), 'admin'))
    WITH CHECK (public.has_role(auth.uid(), 'admin'));

-- 6. USERS TABLE (PROFILES)
-- ============================================
CREATE TABLE public.users (
    id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
    email VARCHAR(255) NOT NULL UNIQUE,
    display_name VARCHAR(255) NOT NULL,
    photo_url TEXT,
    phone_number VARCHAR(20),
    email_verified BOOLEAN DEFAULT false,
    preferences JSONB DEFAULT '{}',
    metadata JSONB DEFAULT '{}',
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE INDEX idx_users_email ON users(email);

CREATE TRIGGER update_users_updated_at
    BEFORE UPDATE ON users
    FOR EACH ROW
    EXECUTE FUNCTION update_updated_at_column();

ALTER TABLE users ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view their own profile"
    ON users FOR SELECT
    TO authenticated
    USING (auth.uid() = id);

CREATE POLICY "Users can update their own profile"
    ON users FOR UPDATE
    TO authenticated
    USING (auth.uid() = id)
    WITH CHECK (auth.uid() = id);

CREATE POLICY "Admins can view all users"
    ON users FOR SELECT
    TO authenticated
    USING (public.has_role(auth.uid(), 'admin'));

-- Auto-create user profile on signup
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS TRIGGER
LANGUAGE PLPGSQL
SECURITY DEFINER
SET search_path = public
AS $$
BEGIN
  INSERT INTO public.users (id, email, display_name, email_verified)
  VALUES (
    NEW.id, 
    NEW.email, 
    COALESCE(NEW.raw_user_meta_data->>'display_name', SPLIT_PART(NEW.email, '@', 1)),
    NEW.email_confirmed_at IS NOT NULL
  );
  
  -- Assign default 'client' role
  INSERT INTO public.user_roles (user_id, role)
  VALUES (NEW.id, 'client');
  
  RETURN NEW;
END;
$$;

DROP TRIGGER IF EXISTS on_auth_user_created ON auth.users;
CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION public.handle_new_user();

-- 7. CATEGORIES TABLE
-- ============================================
CREATE TABLE public.categories (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    slug VARCHAR(100) NOT NULL UNIQUE,
    name VARCHAR(255) NOT NULL,
    description TEXT,
    icon VARCHAR(50),
    image_url TEXT,
    display_order INTEGER DEFAULT 0,
    active BOOLEAN DEFAULT true,
    provider_count INTEGER DEFAULT 0,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE INDEX idx_categories_slug ON categories(slug);
CREATE INDEX idx_categories_active_order ON categories(active, display_order);

CREATE TRIGGER update_categories_updated_at
    BEFORE UPDATE ON categories
    FOR EACH ROW
    EXECUTE FUNCTION update_updated_at_column();

ALTER TABLE categories ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Anyone can view active categories"
    ON categories FOR SELECT
    TO anon, authenticated
    USING (active = true);

CREATE POLICY "Admins can manage categories"
    ON categories FOR ALL
    TO authenticated
    USING (public.has_role(auth.uid(), 'admin'))
    WITH CHECK (public.has_role(auth.uid(), 'admin'));

-- 8. WILAYAS TABLE
-- ============================================
CREATE TABLE public.wilayas (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    code VARCHAR(2) NOT NULL UNIQUE,
    slug VARCHAR(100) NOT NULL UNIQUE,
    name VARCHAR(255) NOT NULL,
    name_ar VARCHAR(255),
    active BOOLEAN DEFAULT true,
    provider_count INTEGER DEFAULT 0,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE INDEX idx_wilayas_code ON wilayas(code);
CREATE INDEX idx_wilayas_slug ON wilayas(slug);

CREATE TRIGGER update_wilayas_updated_at
    BEFORE UPDATE ON wilayas
    FOR EACH ROW
    EXECUTE FUNCTION update_updated_at_column();

ALTER TABLE wilayas ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Anyone can view active wilayas"
    ON wilayas FOR SELECT
    TO anon, authenticated
    USING (active = true);

CREATE POLICY "Admins can manage wilayas"
    ON wilayas FOR ALL
    TO authenticated
    USING (public.has_role(auth.uid(), 'admin'))
    WITH CHECK (public.has_role(auth.uid(), 'admin'));

-- 9. PROVIDERS TABLE
-- ============================================
CREATE TABLE public.providers (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE UNIQUE,
    business_name VARCHAR(255) NOT NULL,
    category VARCHAR(50) NOT NULL,
    subcategories TEXT[],
    description TEXT NOT NULL,
    wilaya VARCHAR(50) NOT NULL,
    coverage_areas TEXT[],
    rating DECIMAL(3,2) DEFAULT 0.00,
    review_count INTEGER DEFAULT 0,
    price_min INTEGER,
    price_max INTEGER,
    price_currency VARCHAR(10) DEFAULT 'DZD',
    portfolio_images TEXT[],
    services TEXT[],
    contact_email VARCHAR(255) NOT NULL,
    contact_phone VARCHAR(20),
    contact_whatsapp VARCHAR(20),
    contact_address TEXT,
    contact_website TEXT,
    social_facebook TEXT,
    social_instagram TEXT,
    social_tiktok TEXT,
    verified BOOLEAN DEFAULT false,
    featured BOOLEAN DEFAULT false,
    status VARCHAR(20) DEFAULT 'pending',
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    CONSTRAINT providers_status_check CHECK (status IN ('pending', 'active', 'inactive', 'suspended')),
    CONSTRAINT providers_rating_check CHECK (rating >= 0 AND rating <= 5)
);

CREATE INDEX idx_providers_user_id ON providers(user_id);
CREATE INDEX idx_providers_category_wilaya ON providers(category, wilaya, status);
CREATE INDEX idx_providers_rating ON providers(rating DESC);
CREATE INDEX idx_providers_featured ON providers(featured, rating DESC) WHERE featured = true;
CREATE INDEX idx_providers_status ON providers(status, created_at DESC);

CREATE TRIGGER update_providers_updated_at
    BEFORE UPDATE ON providers
    FOR EACH ROW
    EXECUTE FUNCTION update_updated_at_column();

ALTER TABLE providers ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Anyone can view active providers"
    ON providers FOR SELECT
    TO anon, authenticated
    USING (status = 'active');

CREATE POLICY "Providers can view their own profile"
    ON providers FOR SELECT
    TO authenticated
    USING (user_id = auth.uid());

CREATE POLICY "Providers can create their profile"
    ON providers FOR INSERT
    TO authenticated
    WITH CHECK (user_id = auth.uid() AND public.has_role(auth.uid(), 'provider'));

CREATE POLICY "Providers can update their own profile"
    ON providers FOR UPDATE
    TO authenticated
    USING (user_id = auth.uid())
    WITH CHECK (user_id = auth.uid());

CREATE POLICY "Admins can manage all providers"
    ON providers FOR ALL
    TO authenticated
    USING (public.has_role(auth.uid(), 'admin'))
    WITH CHECK (public.has_role(auth.uid(), 'admin'));

-- 10. BOOKINGS TABLE
-- ============================================
CREATE TABLE public.bookings (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    client_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    provider_id UUID NOT NULL REFERENCES providers(id) ON DELETE CASCADE,
    event_type VARCHAR(50) NOT NULL,
    event_date TIMESTAMP WITH TIME ZONE NOT NULL,
    wilaya VARCHAR(50) NOT NULL,
    guest_count INTEGER,
    budget_amount INTEGER,
    budget_currency VARCHAR(10) DEFAULT 'DZD',
    message TEXT,
    status VARCHAR(20) DEFAULT 'pending',
    provider_response TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    responded_at TIMESTAMP WITH TIME ZONE,
    CONSTRAINT bookings_event_type_check CHECK (
        event_type IN ('mariage', 'fiancailles', 'anniversaire', 'autre')
    ),
    CONSTRAINT bookings_status_check CHECK (
        status IN ('pending', 'contacted', 'accepted', 'declined', 'completed', 'cancelled')
    )
);

CREATE INDEX idx_bookings_client_id ON bookings(client_id, status);
CREATE INDEX idx_bookings_provider_id ON bookings(provider_id, status);
CREATE INDEX idx_bookings_event_date ON bookings(event_date ASC);
CREATE INDEX idx_bookings_created_at ON bookings(created_at DESC);

CREATE TRIGGER update_bookings_updated_at
    BEFORE UPDATE ON bookings
    FOR EACH ROW
    EXECUTE FUNCTION update_updated_at_column();

ALTER TABLE bookings ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Clients can view their own bookings"
    ON bookings FOR SELECT
    TO authenticated
    USING (client_id = auth.uid());

CREATE POLICY "Providers can view their bookings"
    ON bookings FOR SELECT
    TO authenticated
    USING (provider_id IN (SELECT id FROM providers WHERE user_id = auth.uid()));

CREATE POLICY "Clients can create bookings"
    ON bookings FOR INSERT
    TO authenticated
    WITH CHECK (client_id = auth.uid());

CREATE POLICY "Clients and providers can update their bookings"
    ON bookings FOR UPDATE
    TO authenticated
    USING (
        client_id = auth.uid() OR 
        provider_id IN (SELECT id FROM providers WHERE user_id = auth.uid())
    );

CREATE POLICY "Admins can manage all bookings"
    ON bookings FOR ALL
    TO authenticated
    USING (public.has_role(auth.uid(), 'admin'))
    WITH CHECK (public.has_role(auth.uid(), 'admin'));

-- 11. REVIEWS TABLE
-- ============================================
CREATE TABLE public.reviews (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    client_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    provider_id UUID NOT NULL REFERENCES providers(id) ON DELETE CASCADE,
    booking_id UUID REFERENCES bookings(id) ON DELETE SET NULL UNIQUE,
    rating INTEGER NOT NULL,
    title VARCHAR(255),
    comment TEXT NOT NULL,
    photos TEXT[],
    helpful_count INTEGER DEFAULT 0,
    verified BOOLEAN DEFAULT false,
    status VARCHAR(20) DEFAULT 'pending',
    provider_response TEXT,
    provider_responded_at TIMESTAMP WITH TIME ZONE,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    CONSTRAINT reviews_rating_check CHECK (rating >= 1 AND rating <= 5),
    CONSTRAINT reviews_status_check CHECK (status IN ('pending', 'approved', 'rejected'))
);

CREATE INDEX idx_reviews_provider_id ON reviews(provider_id, status, created_at DESC);
CREATE INDEX idx_reviews_client_id ON reviews(client_id, created_at DESC);
CREATE INDEX idx_reviews_rating ON reviews(rating DESC);

CREATE TRIGGER update_reviews_updated_at
    BEFORE UPDATE ON reviews
    FOR EACH ROW
    EXECUTE FUNCTION update_updated_at_column();

-- Function to update provider rating
CREATE OR REPLACE FUNCTION update_provider_rating()
RETURNS TRIGGER AS $$
BEGIN
    UPDATE providers
    SET 
        rating = (
            SELECT COALESCE(ROUND(AVG(rating)::numeric, 2), 0)
            FROM reviews
            WHERE provider_id = NEW.provider_id AND status = 'approved'
        ),
        review_count = (
